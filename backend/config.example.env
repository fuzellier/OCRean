# OCRean API Configuration

# Storage Backend Configuration
# Options: "local" or "s3"
STORAGE_BACKEND=s3

# Local Storage Configuration (used when STORAGE_BACKEND=local)
LOCAL_DATA_DIR=data

# S3 Storage Configuration (used when STORAGE_BACKEND=s3)
# Bucket name - get this from Terraform output: terraform output -raw file_storage_bucket_name
S3_BUCKET_NAME=ocrean-dev-files
S3_REGION=eu-west-3

# AWS Credentials
# Option 1: Use AWS CLI profile (recommended)
AWS_PROFILE=maxence-dev

# Option 2: Leave empty to use default credential chain:
#   - Environment variable AWS_PROFILE
#   - AWS CLI default credentials (~/.aws/credentials)
#   - IAM role (for EC2/ECS deployments)

# OCR Configuration
USE_GPU=true
